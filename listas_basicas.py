numeros = [10, 20, 30, 40, 50, "Roberto"]
# numeros.append(60) # Con .append lo metemos al final
# numeros.pop(0) # Con .pop(posicion) lo eliminamos

numeros.append(60)
# numeros.append(70)
# numeros.insert(0, 5) # Con .insert(posicion, valor) lo metemos en la posicion que queramos
# numeros.insert(2, 15)
numeros.pop(0)
numeros.remove("Roberto")
print(numeros)